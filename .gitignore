# macOS
.DS_Store

# Worktrees Claude Code (locaux uniquement, ne pas committer)
.claude/worktrees/

# Build output
dist/
node_modules/
package-lock.json

# Backup local des images avant compression WebP (ne pas committer, énorme)
.image-backup-before-webp/

# Mapping temporaire généré par le script de self-host
.asset-mapping.json

# Téléchargements temporaires
.tmp-downloads/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Config MySQL admin (o2switch) — jamais committé, contient le mot de passe DB
admin-auth/config.php
